# Feature Configuration System

This system allows you to easily enable/disable functionality across the entire application.

## Usage

### 1. Modify Feature Config

Edit `web/src/config/features.ts` to enable/disable features:
